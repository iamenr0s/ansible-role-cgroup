---

- name: Reboot host for cgroup changes
  ansible.builtin.reboot:
    reboot_timeout: 600
    msg: "Reboot initiated to apply cgroup configuration"
  when: cgroup_allow_reboot | default(true) | bool
